generuje database typescript
npx supabase gen types --lang=typescript --project-id process.env.EXPO_PUBLIC_KEY_SUPABASE_PROJECT --schema public > src/database.types.ts


npx supabase gen types --lang=typescript --project-id vpululxkipxqqgnzycnc --schema public > src/database.types.ts

npm install supabase --save-dev

npx supabase link --project-ref vpululxkipxqqgnzycnc



# Configure environment (Tanstack Router, Tanstack Query, TailwindCSS, Shadcn/UI)

# Setup Supabase (create database, create tables, generate types)

# Create Login/Register/Profile/Admin page

# Create Insert Products page
# Cart -> add/remove items, různé cenové relace podle role uživatele
# cartStore.tsx - totalMobil, total
# ProductCategory.tsx - priceMobil, price

# Checkout -> create order, add items to order, remove items from order
# Order -> list of orders
# Order Details -> details of order
# Order Items -> list of order items

# Create driver page

